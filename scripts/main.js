/* AI Dungeon Crawler — Main bootstrap
 *
 * Stage 1a scaffold. Implementation lands in later stages.
 * Entry point: loads templates, fetches /api/config, calls PackLoader,
 * hydrates GameState, mounts UI, wires input events.
 */
(function (global) {
    'use strict';

    global.Main = {};
})(typeof window !== 'undefined' ? window : globalThis);
